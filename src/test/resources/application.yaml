logging:
  pattern:
    console: "%clr([${LOG_LEVEL_PATTERN:%5p})] %clr(%-40.40logger{10}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:%wEx}"
camel:
  springboot:
    main-run-controller: true
spring:
  application:
    name: name
  profiles:
    active: test
